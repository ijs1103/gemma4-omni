# React Native & Metro compatibility configuration
# Hoist all dependencies to the root node_modules to ensure Metro can resolve nested transitive dependencies
shamefully-hoist=true
public-hoist-pattern[]=*
